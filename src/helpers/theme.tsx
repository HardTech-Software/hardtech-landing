export interface ThemeProps {
  primary: string;
  secondary: string;
  background: string;
  white: string;
}

export const theme: ThemeProps = {
  primary: "#FE71F4",
  secondary: "#2441FF",
  background: "#202020",
  white: "#ffffff",
};
